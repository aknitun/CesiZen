{% extends 'base.html.twig' %}

{% block title %}Contenu{% endblock %}

{% block body %}
    <h1>Contenu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contenu.id }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ contenu.slug }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ contenu.titre }}</td>
            </tr>
            <tr>
                <th>Corps</th>
                <td>{{ contenu.corps }}</td>
            </tr>
            <tr>
                <th>Publier</th>
                <td>{{ contenu.publier ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contenu_index') }}">back to list</a>

    <a href="{{ path('app_contenu_edit', {'id': contenu.id}) }}">edit</a>

    {{ include('contenu/_delete_form.html.twig') }}
{% endblock %}
