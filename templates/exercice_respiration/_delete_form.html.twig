<form method="post" action="{{ path('app_exercice_respiration_delete', {'id': exercice_respiration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ exercice_respiration.id) }}">
    <button class="btn">Delete</button>
</form>
